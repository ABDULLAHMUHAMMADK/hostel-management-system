info:
  name: remove student
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:5000/api/hostel/remove-student
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
